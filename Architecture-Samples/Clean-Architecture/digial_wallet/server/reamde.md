create the directory manually
add the pubspec 
server the main file as "server.dart"
to start the server:
`dart run bin/server.dart`
to test the server use run the `curl http://localhost:8080`
Ctrl+C to stop the server
Before using the app start the server


