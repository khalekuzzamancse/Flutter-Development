name: server
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=3.3.0 <4.0.0'
dependencies:
  shelf: ^1.3.0  # For handling HTTP requests
  shelf_router: ^1.0.0  # For routing requests in your server
dev_dependencies:
  test: ^1.16.0
executables:
  dart_server:
