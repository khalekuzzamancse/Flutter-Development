import 'package:flutter/widgets.dart';
class MeasuredChild {
  final String id;
  final Size size;
  final Size parentSize;
  MeasuredChild({required this.id, required this.size, required this.parentSize});
}